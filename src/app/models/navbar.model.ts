export interface NavBar {
    /**
     * Creates a NavBar.
     * @param {string} logo - logo src.
     * @param {string} navLink - link route.
     * @param {string} navLinkText - link text.
    */
    logo: string;
    navLink: string;
    navLinkText: string;
}